info:
  name: get_sessions
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/f1/season/{{season}}/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
