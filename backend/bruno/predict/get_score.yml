info:
  name: get_score
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/game/{{game_id}}/f1session/{{session_id}}/score"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
